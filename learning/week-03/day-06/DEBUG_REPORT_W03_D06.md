# DEBUG REPORT #1 — W03D06

Complete only with learner-performed, learner-supplied, or tool-verified facts.
Preserve failure evidence and measure before fixing. Do not infer missing facts.

## Metadata

- Date: `2026-08-29`
- Commit / branch: `<learner records current commit / branch after experiment>`
- Hardware/toolchain if applicable: `<learner fills after experiment, or NOT APPLICABLE>`
- AI assistance used: `<learner records actual assistance and timing>`
- Fault category: `<learner selects one category>`

## 1. Symptom

`<learner fills after experiment>`

## 2. Expected

`<learner fills before interpreting the failure>`

## 3. Reproduction

`<learner fills with the exact reproducible case and commands/setup>`

## 4. Known facts

`<learner fills from evidence>`

## 5. Unknowns

`<learner fills>`

## 6. Hypotheses

`<learner fills before fixing>`

## 7. Measurements

`<learner fills with measurement chosen, method, raw result, and hypothesis update>`

## 8. Root cause

`<learner fills only after evidence supports the conclusion>`

## 9. Minimal fix

`<learner fills only after measurement>`

## 10. Regression test

`<learner fills with exact check, expected result, actual result, and exit code>`

## 11. Side effects

`<learner fills after the fix and regression>`

## 12. Remaining limitation

`<learner fills; use NONE only with evidence>`

## 13. Evidence links

- Raw terminal/test log: `<learner records path>`
- Failure screenshot/capture if applicable: `<learner records path or NOT PERFORMED>`
- Measurement evidence: `<learner records path>`
- Regression output: `<learner records path>`
- Relevant source diff/commit: `<learner records reference>`

## 14. Learner self-explanation

`<learner explains symptom -> expected -> hypothesis -> measurement -> root cause -> minimal fix -> regression in own words>`

## 15. AI provenance

- Meaningful learner attempt completed before technical AI review: `<learner records YES/NO and evidence>`
- Symptom supplied before technical AI review: `<learner records YES/NO>`
- Expected behavior supplied before technical AI review: `<learner records YES/NO>`
- Hypothesis and/or measurement supplied before technical AI review: `<learner records YES/NO>`
- Highest AI level actually used: `<learner records>`
- Exact AI/executor contribution: `<learner records>`
- Core technical patch supplied by AI/executor: `<learner records YES/NO>`
- Competency implication: `<record truthfully; no competency result is created by this template>`

## 16. Next Action

`<learner writes one concrete 5–15 minute NEXT ACTION>`
